services:
  - type: web
    name: backend
    env: dotnet
    plan: free
    buildCommand: "dotnet publish -c Release -o out"
    startCommand: "dotnet out/YoutubeDownload.Back.dll"
    autoDeploy: true
